package ${package_name}.service;

import com.jokey.bingo.mvc.BaseService;
import ${package_name}.entity.${table_name};

/**
 * @author ${author}
 * Date ${date?string('yyyy年M月d日')}
 * Version 1.0
 */
public interface ${table_name}Service extends BaseService<${table_name}> {
}